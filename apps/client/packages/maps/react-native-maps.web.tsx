const MapView = () => {
  // eslint-disable-next-line no-console
  console.log(`"react-native-maps" cannot be used for web!`)

  return null
}

export default MapView

export const Marker = MapView

type MapPressEvent = never

export type { MapPressEvent }
